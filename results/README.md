# Results

Most files here are **generated** and **gitignored** (GeoTIFF, JSON, CSV, PNG). Only **`*.md`** files under `results/` are tracked in git.

| Path | Role |
|------|------|
| `walkthrough.md` | Main lab notebook: steps 1–5, scripts, checkpoints |
| `step_01_inputs/` … `step_05_mass/` | Per-step README checkpoints (see walkthrough) |
| `palisades/` | Default output folder for `scripts/palisades_pipeline.py` (`--out` defaults here) |

Re-run pipelines locally to regenerate rasters and summaries after changing inputs or parameters.
